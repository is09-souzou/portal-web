import Tab    from "@material-ui/core/Tab";
import styled from "styled-components";

export default styled(Tab)`
    && {
        width: 50%;
        > :hover {
            color: #ff9100;
        }
    }
`;
